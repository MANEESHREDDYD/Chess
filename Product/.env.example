# Stage 0 has no required env vars — the app runs entirely client-side.
# These placeholders document what Stage 1 (Agent G) will need.

# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=
# VITE_SENTRY_DSN=

# Optional during MVP until the human provides the production Google Form.
# VITE_FEEDBACK_FORM_URL=https://forms.gle/your-form-id
