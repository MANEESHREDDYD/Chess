# Agent Briefs

The source of truth for the eight implementation briefs is
`docs/v4_implementation.html`, section 7.

Use this folder for extracted per-agent Markdown briefs as the project moves
past Agent A. Keep the HTML plan as the canonical reference until those files
are split out.
